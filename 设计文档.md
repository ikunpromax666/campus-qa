# 在线校园问答互助系统 - 设计文档

## 1. 技术架构设计

### 1.1 技术选型

| 层次 | 技术 | 版本 | 说明 |
|------|------|------|------|
| **基础框架** | Spring Boot | 2.7.x | Web应用框架 |
| **ORM框架** | MyBatis-Plus | 3.5.x | 简化CRUD操作 |
| **数据库** | MySQL | 8.0 | 关系型数据库 |
| **缓存** | Redis | 6.0+ | 缓存热点数据、排行榜 |
| **接口文档** | Swagger/Knife4j | 3.0 | 接口文档自动生成 |
| **JWT** | jjwt | 0.9.x | Token认证 |
| **工具类** | Hutool | 5.x | 常用工具方法 |
| **参数校验** | Validation | - | 接口参数校验 |

### 1.2 项目结构

```
campus-qa/
├── sql/                          # 数据库脚本
│   └── schema.sql
├── docs/                         # 项目文档
│   ├── 需求文档.md
│   └── 设计文档.md
├── campus-qa-backend/            # 后端项目
│   ├── pom.xml
│   ├── src/main/java/com/campus/qa/
│   │   ├── CampusQaApplication.java
│   │   ├── common/               # 公共模块
│   │   │   ├── result/          # 统一返回结果
│   │   │   ├── enums/           # 枚举类
│   │   │   ├── exception/       # 全局异常处理
│   │   │   └── utils/           # 工具类
│   │   ├── config/              # 配置类
│   │   │   ├── CorsConfig.java
│   │   │   ├── RedisConfig.java
│   │   │   ├── SwaggerConfig.java
│   │   │   └── WebMvcConfig.java
│   │   ├── interceptor/         # 拦截器
│   │   │   └── LoginInterceptor.java
│   │   ├── controller/          # 控制器层
│   │   │   ├── UserController.java
│   │   │   ├── QuestionController.java
│   │   │   ├── AnswerController.java
│   │   │   ├── LikeController.java
│   │   │   ├── FavoriteController.java
│   │   │   ├── SearchController.java
│   │   │   ├── RankController.java
│   │   │   └── admin/           # 管理后台
│   │   │       ├── AdminUserController.java
│   │   │       ├── AdminCategoryController.java
│   │   │       └── AdminTagController.java
│   │   ├── service/             # 业务逻辑层
│   │   │   ├── UserService.java
│   │   │   ├── QuestionService.java
│   │   │   ├── AnswerService.java
│   │   │   ├── LikeService.java
│   │   │   ├── FavoriteService.java
│   │   │   ├── SearchService.java
│   │   │   ├── RankService.java
│   │   │   └── impl/           # 实现类
│   │   ├── mapper/              # 数据访问层
│   │   │   ├── UserMapper.java
│   │   │   ├── QuestionMapper.java
│   │   │   ├── AnswerMapper.java
│   │   │   ├── CategoryMapper.java
│   │   │   ├── TagMapper.java
│   │   │   ├── LikeRecordMapper.java
│   │   │   └── FavoriteMapper.java
│   │   ├── entity/              # 实体类
│   │   │   ├── User.java
│   │   │   ├── Question.java
│   │   │   ├── Answer.java
│   │   │   ├── Category.java
│   │   │   ├── Tag.java
│   │   │   ├── LikeRecord.java
│   │   │   ├── Favorite.java
│   │   │   └── QuestionTag.java
│   │   ├── dto/                 # 数据传输对象
│   │   │   ├── request/        # 请求参数
│   │   │   └── response/       # 返回结果
│   │   └── vo/                  # 视图对象
│   └── src/main/resources/
│       ├── application.yml
│       ├── mapper/              # MyBatis XML
│       └── static/              # 静态资源
└── campus-qa-admin/             # 管理后台前端（后期）
```

---

## 2. 数据库设计

### 2.1 ER关系图

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│     user     │     │   category   │     │     tag      │
├──────────────┤     ├──────────────┤     ├──────────────┤
│ id (PK)      │     │ id (PK)      │     │ id (PK)      │
│ phone        │     │ name         │     │ name         │
│ password     │     │ description  │     └──────────────┘
│ nickname     │     │ sort_order   │           │
│ avatar       │     └──────────────┘           │
│ bio          │            │                   │
│ role         │            │                   │
│ create_time  │            │                   │
│ update_time  │            │                   │
└──────────────┘            │                   │
       │                    │                   │
       │ 1:N                │ N:1               │ N:M
       ↓                    ↓                   ↓
┌──────────────────────────────────────┐  ┌──────────────┐
│            question                  │  │ question_tag │
├──────────────────────────────────────┤  ├──────────────┤
│ id (PK)                             │  │ id (PK)      │
│ user_id (FK) → user.id              │  │ question_id  │
│ title                               │  │ tag_id       │
│ content                             │  └──────────────┘
│ category_id (FK) → category.id      │
│ view_count                          │
│ like_count                          │
│ answer_count                        │
│ is_top                              │
│ status                              │
│ create_time                         │
│ update_time                         │
└──────────────────────────────────────┘
       │                    │
       │ 1:N                │ 1:N
       ↓                    ↓
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│    answer    │    │ like_record  │    │   favorite   │
├──────────────┤    ├──────────────┤    ├──────────────┤
│ id (PK)      │    │ id (PK)      │    │ id (PK)      │
│ question_id  │    │ user_id      │    │ user_id      │
│ user_id      │    │ target_id    │    │ question_id  │
│ content      │    │ target_type  │    │ create_time  │
│ like_count   │    │ create_time  │    └──────────────┘
│ is_accepted  │    └──────────────┘
│ status       │
│ create_time  │
│ update_time  │
└──────────────┘
```

### 2.2 索引设计

| 表名 | 索引名 | 索引字段 | 索引类型 | 说明 |
|------|--------|---------|---------|------|
| user | uk_phone | phone | UNIQUE | 手机号唯一 |
| question | idx_user_id | user_id | 普通 | 用户问题查询 |
| question | idx_category_id | category_id | 普通 | 分类筛选 |
| question | idx_create_time | create_time | 普通 | 时间排序 |
| question_tag | uk_question_tag | question_id, tag_id | UNIQUE | 防重复 |
| question_tag | idx_tag_id | tag_id | 普通 | 标签查询 |
| answer | idx_question_id | question_id | 普通 | 问题回答列表 |
| answer | idx_user_id | user_id | 普通 | 用户回答列表 |
| like_record | uk_user_target | user_id, target_id | UNIQUE | 防重复点赞 |
| like_record | idx_target | target_id | 普通 | 目标查询 |
| favorite | uk_user_question | user_id, question_id | UNIQUE | 防重复收藏 |
| favorite | idx_question_id | question_id | 普通 | 问题收藏查询 |

---

## 3. 接口设计

### 3.1 接口规范

- **基础路径**：`/api`
- **认证方式**：JWT Token，通过`Authorization`请求头传递
- **返回格式**：统一JSON格式
- **分页参数**：`page`（页码，从1开始）、`size`（每页条数）

### 3.2 统一返回格式

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

**错误码定义**：

| code | 说明 |
|------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

### 3.3 用户模块接口

#### 3.3.1 用户注册

```
POST /api/user/register
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| phone | String | 是 | 手机号 |
| password | String | 是 | 密码 |
| confirmPassword | String | 是 | 确认密码 |
| nickname | String | 是 | 昵称 |

**请求示例**：
```json
{
  "phone": "13800138000",
  "password": "123456",
  "confirmPassword": "123456",
  "nickname": "张三"
}
```

**返回示例**：
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1
  }
}
```

#### 3.3.2 用户登录

```
POST /api/user/login
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| phone | String | 是 | 手机号 |
| password | String | 是 | 密码 |

**返回示例**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "nickname": "张三"
  }
}
```

#### 3.3.3 获取当前用户信息

```
GET /api/user/info
```

**请求头**：
```
Authorization: Bearer {token}
```

**返回示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "phone": "13800138000",
    "nickname": "张三",
    "avatar": "https://...",
    "bio": "这个人很懒",
    "role": 0,
    "createTime": "2024-01-01 10:00:00"
  }
}
```

#### 3.3.4 修改个人信息

```
PUT /api/user/info
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| nickname | String | 否 | 昵称 |
| avatar | String | 否 | 头像URL |
| bio | String | 否 | 个人简介 |

#### 3.3.5 修改密码

```
PUT /api/user/password
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| oldPassword | String | 是 | 原密码 |
| newPassword | String | 是 | 新密码 |
| confirmPassword | String | 是 | 确认密码 |

---

### 3.4 问题模块接口

#### 3.4.1 发布问题

```
POST /api/question
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| title | String | 是 | 标题（5-150字符） |
| content | String | 是 | 内容（Markdown） |
| categoryId | Long | 是 | 分类ID |
| tagIds | List<Long> | 否 | 标签ID列表 |

#### 3.4.2 问题列表

```
GET /api/question/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页条数，默认10 |
| categoryId | Long | 否 | 分类ID |
| keyword | String | 否 | 搜索关键词 |
| sort | String | 否 | 排序：time(默认)/hot |

**返回示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "Spring Boot如何集成Redis?",
        "nickname": "张三",
        "avatar": "...",
        "categoryName": "技术交流",
        "tags": ["Java", "Redis"],
        "viewCount": 100,
        "likeCount": 15,
        "answerCount": 5,
        "createTime": "2024-01-01 10:00:00"
      }
    ],
    "total": 100,
    "page": 1,
    "size": 10
  }
}
```

#### 3.4.3 问题详情

```
GET /api/question/{id}
```

**返回示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "Spring Boot如何集成Redis?",
    "content": "# 问题描述\n...",
    "userId": 1,
    "nickname": "张三",
    "avatar": "...",
    "category": {
      "id": 1,
      "name": "技术交流"
    },
    "tags": [
      {"id": 1, "name": "Java"},
      {"id": 2, "name": "Redis"}
    ],
    "viewCount": 100,
    "likeCount": 15,
    "answerCount": 5,
    "isTop": false,
    "createTime": "2024-01-01 10:00:00",
    "isLiked": false,
    "isDisliked": false,
    "isFavorited": false,
    "isOwner": true
  }
}
```

#### 3.4.4 编辑问题

```
PUT /api/question/{id}
```

#### 3.4.5 删除问题

```
DELETE /api/question/{id}
```

#### 3.4.6 关闭问题

```
PUT /api/question/{id}/close
```

---

### 3.5 回答模块接口

#### 3.5.1 发布回答

```
POST /api/answer
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| questionId | Long | 是 | 问题ID |
| content | String | 是 | 回答内容 |

#### 3.5.2 问题的回答列表

```
GET /api/answer/list
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| questionId | Long | 是 | 问题ID |
| page | Integer | 否 | 页码，默认1 |
| size | Integer | 否 | 每页条数，默认10 |

#### 3.5.3 编辑回答

```
PUT /api/answer/{id}
```

#### 3.5.4 删除回答

```
DELETE /api/answer/{id}
```

#### 3.5.5 采纳最佳答案

```
PUT /api/answer/{id}/accept
```

---

### 3.6 互动模块接口

#### 3.6.1 点赞/取消点赞

```
POST /api/like/toggle
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| targetId | Long | 是 | 目标ID |
| targetType | Integer | 是 | 类型：1问题 2回答 |

**返回示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "liked": true,
    "likeCount": 16
  }
}
```

#### 3.6.2 踩/取消踩

```
POST /api/dislike/toggle
```

参数同点赞。

#### 3.6.3 收藏/取消收藏

```
POST /api/favorite/toggle
```

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| questionId | Long | 是 | 问题ID |

---

### 3.7 排行榜接口

#### 3.7.1 热门问题榜

```
GET /api/rank/hot
```

**返回示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "title": "...",
      "viewCount": 100,
      "likeCount": 15,
      "answerCount": 5
    }
  ]
}
```

#### 3.7.2 优秀回答者榜

```
GET /api/rank/answerer
```

---

### 3.8 管理后台接口

#### 3.8.1 用户管理

```
GET    /api/admin/user/list        # 用户列表
PUT    /api/admin/user/{id}/status # 禁用/启用用户
```

#### 3.8.2 分类管理

```
GET    /api/admin/category/list    # 分类列表
POST   /api/admin/category         # 新增分类
PUT    /api/admin/category/{id}    # 编辑分类
DELETE /api/admin/category/{id}    # 删除分类
```

#### 3.8.3 标签管理

```
GET    /api/admin/tag/list         # 标签列表
POST   /api/admin/tag              # 新增标签
DELETE /api/admin/tag/{id}         # 删除标签
```

---

## 4. 缓存设计（Redis）

### 4.1 缓存场景

| 场景 | 缓存策略 | 过期时间 |
|------|---------|---------|
| 热门问题榜 | String/JSON | 10分钟 |
| 优秀回答者榜 | String/JSON | 30分钟 |
| 问题浏览量 | Hash | 持久化到DB |
| 点赞/踩计数 | Hash | 持久化到DB |
| 用户Token | String | 7天 |
| 问题详情 | String/JSON | 5分钟 |

### 4.2 Key设计

```
# 热门问题榜
rank:hot                          → List<String>

# 优秀回答者榜
rank:answerer                     → List<String>

# 问题浏览量
question:view:{questionId}        → Integer

# 问题点赞数
question:like:{questionId}        → Integer

# 回答点赞数
answer:like:{answerId}            → Integer

# 用户是否已点赞
user:like:{userId}:{targetId}     → 1/0

# 用户Token
user:token:{userId}               → String

# 问题详情缓存
question:detail:{questionId}      → String(JSON)
```

### 4.3 缓存与数据库同步

**浏览量、点赞数的同步策略**：

```
写入Redis（实时）
      ↓
定时任务（每5分钟）同步到MySQL
      ↓
清除Redis缓存
```

**代码示例**：
```java
@Scheduled(fixedRate = 5 * 60 * 1000) // 5分钟执行一次
public void syncCountToDb() {
    // 遍历Redis中的计数
    // 更新到MySQL
    // 清除Redis缓存
}
```

---

## 5. 安全设计

### 5.1 认证流程

```
1. 用户登录 → 验证手机号密码
2. 生成JWT Token（包含userId、过期时间）
3. 返回Token给前端
4. 前端每次请求携带Token
5. 拦截器验证Token有效性
6. 将userId放入Request，供后续使用
```

### 5.2 JWT Token设计

**Token结构**：
```
Header.Payload.Signature
```

**Payload内容**：
```json
{
  "userId": 1,
  "role": 0,
  "exp": 1704067200
}
```

**Token有效期**：7天

### 5.3 接口权限控制

| 接口类型 | 权限要求 |
|---------|---------|
| 注册、登录 | 无需登录 |
| 问题列表、详情、搜索、排行榜 | 无需登录 |
| 发布问题/回答、点赞、收藏、采纳 | 需要登录 |
| 编辑/删除问题/回答 | 需要登录 + 是作者 |
| 管理后台接口 | 需要登录 + 管理员角色 |

### 5.4 参数校验

使用`@Valid`注解进行参数校验：

```java
@PostMapping("/register")
public Result register(@RequestBody @Valid UserRegisterDTO dto) {
    // 参数校验失败会自动抛出异常
    // 全局异常处理器捕获并返回友好提示
}
```

---

## 6. 核心业务流程设计

### 6.1 点赞流程

```
用户点击点赞
      ↓
检查是否已点赞（Redis）
      ↓
  ┌─已点赞─→ 取消点赞 → 删除记录 → 计数-1
  │
  └─未点赞─→ 检查是否已踩 → 已踩则先取消踩
                    ↓
              新增点赞记录 → 计数+1
```

### 6.2 采纳流程

```
问题作者点击采纳
      ↓
验证当前用户是问题作者
      ↓
验证回答属于该问题
      ↓
将该回答is_accepted设为1
      ↓
取消该问题其他已采纳回答
      ↓
更新问题is_adopted标记（可选）
```

### 6.3 热度计算公式

```
热度分 = 点赞数 × 2 + 浏览量 × 0.1 + 回答数 × 3

示例：
点赞10，浏览100，回答5
热度 = 10×2 + 100×0.1 + 5×3 = 20 + 10 + 15 = 45
```

---

## 7. 异常处理设计

### 7.1 全局异常处理

```java
@RestControllerAdvice
public class GlobalExceptionHandler {

    @ExceptionHandler(BusinessException.class)
    public Result handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());
    }

    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result handleValidException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldError().getDefaultMessage();
        return Result.error(400, message);
    }

    @ExceptionHandler(Exception.class)
    public Result handleException(Exception e) {
        log.error("系统异常", e);
        return Result.error(500, "系统繁忙，请稍后重试");
    }
}
```

### 7.2 业务异常枚举

```java
public enum ErrorCode {
    // 通用
    SUCCESS(200, "成功"),
    PARAM_ERROR(400, "参数错误"),
    UNAUTHORIZED(401, "未登录"),
    FORBIDDEN(403, "无权限"),
    NOT_FOUND(404, "资源不存在"),

    // 用户相关
    PHONE_EXISTS(1001, "手机号已注册"),
    PHONE_NOT_FOUND(1002, "手机号未注册"),
    PASSWORD_ERROR(1003, "密码错误"),

    // 问题相关
    QUESTION_NOT_FOUND(2001, "问题不存在"),
    QUESTION_CLOSED(2002, "问题已关闭"),
    NOT_QUESTION_OWNER(2003, "非问题作者"),

    // 回答相关
    ANSWER_NOT_FOUND(3001, "回答不存在"),
    NOT_ANSWER_OWNER(3002, "非回答作者"),
}
```

---

## 8. 部署设计

### 8.1 开发环境

| 组件 | 版本 | 说明 |
|------|------|------|
| JDK | 11 | Java运行环境 |
| Maven | 3.6+ | 项目构建 |
| MySQL | 8.0 | 数据库 |
| Redis | 6.0+ | 缓存 |

### 8.2 生产环境（可选）

| 组件 | 说明 |
|------|------|
| 服务器 | 阿里云/腾讯云 2核4G |
| Nginx | 反向代理、静态资源 |
| Docker | 容器化部署 |
