# 在线校园问答互助系统 - 需求文档

## 1. 项目概述

### 1.1 项目背景

校园问答互助系统是一个面向在校学生的知识分享平台，学生可以在平台上提出问题、回答问题、互相帮助，促进校园内的学习交流和知识共享。

### 1.2 项目目标

- 为学生提供一个便捷的问题提问和解答平台
- 促进校园内的知识共享和学习交流
- 建立优质内容的沉淀和检索机制
- 培养学生的互助精神和社区意识

### 1.3 项目范围

- Web端应用（PC浏览器访问）
- 后端服务API
- 管理后台（内容管理、用户管理）

---

## 2. 用户角色定义

### 2.1 普通用户

**定义**：所有注册用户，默认角色

**权限**：
- 注册、登录、修改个人信息
- 发布问题、编辑/删除自己的问题
- 回答问题、编辑/删除自己的回答
- 点赞/踩问题和回答
- 采纳自己问题下的最佳答案
- 收藏问题
- 搜索问题和回答
- 查看个人主页（我的提问、我的回答、我的收藏）

### 2.2 管理员

**定义**：系统维护人员，由超管指定

**权限**：
- 普通用户的所有权限
- 用户管理（禁用/启用用户）
- 分类管理（增删改分类）
- 标签管理（增删改标签）
- 问题审核（违规内容处理）
- 置顶/取消置顶问题
- 数据统计查看

---

## 3. 功能需求

### 3.1 用户模块

#### 3.1.1 用户注册

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 用户注册 |
| 输入 | 手机号、密码、确认密码、昵称 |
| 处理逻辑 | 1. 验证手机号格式<br>2. 检查手机号是否已注册<br>3. 验证密码格式（6-20位）<br>4. 验证两次密码是否一致<br>5. 密码BCrypt加密存储<br>6. 生成用户记录 |
| 输出 | 注册成功返回用户ID，失败返回错误提示 |
| 异常处理 | 手机号已注册、格式错误、密码不一致 |

#### 3.1.2 用户登录

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 用户登录 |
| 输入 | 手机号、密码 |
| 处理逻辑 | 1. 验证手机号是否存在<br>2. 验证密码是否正确<br>3. 生成JWT Token |
| 输出 | 登录成功返回Token，失败返回错误提示 |
| Token有效期 | 7天 |

#### 3.1.3 获取用户信息

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 获取当前登录用户信息 |
| 输入 | Token |
| 处理逻辑 | 1. 解析Token获取用户ID<br>2. 查询用户信息 |
| 输出 | 用户信息（不含密码） |

#### 3.1.4 修改个人信息

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 修改个人信息 |
| 输入 | 昵称、头像URL、个人简介 |
| 处理逻辑 | 1. 验证Token<br>2. 更新用户信息 |
| 输出 | 更新后的用户信息 |

#### 3.1.5 修改密码

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 修改密码 |
| 输入 | 原密码、新密码、确认密码 |
| 处理逻辑 | 1. 验证原密码是否正确<br>2. 验证新密码格式<br>3. 验证两次新密码是否一致<br>4. 更新密码 |
| 输出 | 操作结果 |

---

### 3.2 问题模块

#### 3.2.1 发布问题

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 发布问题 |
| 输入 | 标题、内容（Markdown）、分类ID、标签ID列表 |
| 处理逻辑 | 1. 验证用户登录状态<br>2. 验证标题（5-150字符）<br>3. 验证内容不为空<br>4. 验证分类是否存在<br>5. 验证标签是否存在<br>6. 创建问题记录<br>7. 创建问题-标签关联 |
| 输出 | 问题ID |
| 限制 | 标题5-150字符，内容最大10000字符 |

#### 3.2.2 问题列表

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 分页查询问题列表 |
| 输入 | 页码、每页条数、分类ID（可选）、排序方式（可选） |
| 处理逻辑 | 1. 支持按时间排序（默认）<br>2. 支持按热度排序（点赞数+浏览量）<br>3. 支持按分类筛选<br>4. 支持关键词搜索标题<br>5. 返回问题列表+分页信息<br>6. 置顶问题优先显示 |
| 输出 | 问题列表（不含正文）、总数、分页信息 |
| 默认分页 | 每页10条 |

#### 3.2.3 问题详情

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 获取问题详情 |
| 输入 | 问题ID |
| 处理逻辑 | 1. 查询问题信息<br>2. 浏览量+1<br>3. 查询问题的标签<br>4. 查询问题的分类<br>5. 查询提问者信息<br>6. 判断当前用户是否已点赞/收藏（如已登录） |
| 输出 | 问题完整信息、是否已点赞、是否已收藏 |

#### 3.2.4 编辑问题

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 编辑问题 |
| 输入 | 问题ID、标题、内容、分类ID、标签ID列表 |
| 处理逻辑 | 1. 验证用户是否为问题作者<br>2. 验证参数<br>3. 更新问题信息<br>4. 更新标签关联（先删后增） |
| 输出 | 操作结果 |
| 限制 | 只有作者可以编辑 |

#### 3.2.5 删除问题

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 删除问题（软删除） |
| 输入 | 问题ID |
| 处理逻辑 | 1. 验证用户是否为问题作者或管理员<br>2. 将问题status设为2（已删除） |
| 输出 | 操作结果 |
| 说明 | 软删除，数据不真正删除 |

#### 3.2.6 关闭问题

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 关闭问题 |
| 输入 | 问题ID |
| 处理逻辑 | 1. 验证用户是否为问题作者<br>2. 将问题status设为1（已关闭）<br>3. 关闭后不再接受新回答 |
| 输出 | 操作结果 |

---

### 3.3 回答模块

#### 3.3.1 发布回答

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 回答问题 |
| 输入 | 问题ID、内容（Markdown） |
| 处理逻辑 | 1. 验证用户登录状态<br>2. 验证问题是否存在且状态正常<br>3. 验证内容不为空<br>4. 创建回答记录<br>5. 问题回答数+1 |
| 输出 | 回答ID |

#### 3.3.2 获取问题的回答列表

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 分页查询问题的回答 |
| 输入 | 问题ID、页码、每页条数 |
| 处理逻辑 | 1. 查询该问题的所有回答<br>2. 按时间排序<br>3. 已采纳的回答排在最前<br>4. 返回回答列表+用户信息 |
| 输出 | 回答列表、总数 |

#### 3.3.3 编辑回答

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 编辑回答 |
| 输入 | 回答ID、内容 |
| 处理逻辑 | 1. 验证用户是否为回答作者<br>2. 更新回答内容 |
| 输出 | 操作结果 |

#### 3.3.4 删除回答

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 删除回答（软删除） |
| 输入 | 回答ID |
| 处理逻辑 | 1. 验证用户是否为回答作者或管理员<br>2. 将回答status设为1（已删除）<br>3. 问题回答数-1 |
| 输出 | 操作结果 |

#### 3.3.5 采纳最佳答案

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 采纳最佳答案 |
| 输入 | 问题ID、回答ID |
| 处理逻辑 | 1. 验证用户是否为问题作者<br>2. 验证回答是否属于该问题<br>3. 将该回答设为已采纳<br>4. 取消该问题其他已采纳的回答（如果有）<br>5. 更新question表的is_adopted标记（可选） |
| 输出 | 操作结果 |
| 说明 | 一个问题只能采纳一个回答 |

---

### 3.4 互动模块

#### 3.4.1 点赞/取消点赞

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 点赞或取消点赞 |
| 输入 | 目标ID、目标类型（1问题 2回答） |
| 处理逻辑 | 1. 检查用户是否已点赞<br>2. 已点赞则取消（删除记录，计数-1）<br>3. 未点赞则新增记录（计数+1） |
| 输出 | 当前点赞状态（true/false） |
| 说明 | 使用Redis计数，数据库存记录 |

#### 3.4.2 踩/取消踩

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 踩或取消踩 |
| 输入 | 目标ID、目标类型（1问题 2回答） |
| 处理逻辑 | 1. 检查用户是否已踩<br>2. 已踩则取消<br>3. 未踩则新增记录 |
| 输出 | 当前踩的状态 |
| 说明 | 同一个问题/回答，点赞和踩互斥 |

#### 3.4.3 收藏/取消收藏

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 收藏或取消收藏 |
| 输入 | 问题ID |
| 处理逻辑 | 1. 检查用户是否已收藏<br>2. 已收藏则取消<br>3. 未收藏则新增记录 |
| 输出 | 当前收藏状态（true/false） |

---

### 3.5 搜索模块

#### 3.5.1 关键词搜索

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 搜索问题 |
| 输入 | 关键词、页码、每页条数 |
| 处理逻辑 | 1. 搜索标题包含关键词的问题<br>2. 搜索内容包含关键词的问题<br>3. 返回结果按相关性排序 |
| 输出 | 问题列表、总数 |
| 后期优化 | 可接入Elasticsearch实现全文搜索 |

---

### 3.6 排行榜模块

#### 3.6.1 热门问题榜

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 获取热门问题 |
| 输入 | 无 |
| 处理逻辑 | 1. 查询最近7天的问题<br>2. 按（点赞数×2 + 浏览量×0.1 + 回答数×3）计算热度分<br>3. 返回热度最高的20个问题 |
| 输出 | 热门问题列表 |
| 缓存 | Redis缓存，10分钟过期 |

#### 3.6.2 优秀回答者榜

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 获取优秀回答者排行 |
| 输入 | 无 |
| 处理逻辑 | 1. 统计每个用户的回答数和被采纳数<br>2. 按（回答数×1 + 被采纳数×5）计算积分<br>3. 返回前20名用户 |
| 输出 | 用户列表（昵称、头像、回答数、采纳数） |
| 缓存 | Redis缓存，30分钟过期 |

---

### 3.7 管理后台模块

#### 3.7.1 用户管理

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 用户列表与管理 |
| 输入 | 页码、关键词 |
| 处理逻辑 | 1. 查询用户列表<br>2. 支持按手机号/昵称搜索<br>3. 禁用/启用用户 |
| 输出 | 用户列表、操作结果 |

#### 3.7.2 分类管理

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 分类的增删改 |
| 输入 | 分类ID、名称、描述、排序 |
| 处理逻辑 | 1. 新增分类<br>2. 编辑分类<br>3. 删除分类（如有问题不能删） |
| 输出 | 操作结果 |

#### 3.7.3 标签管理

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 标签的增删改 |
| 输入 | 标签ID、名称 |
| 处理逻辑 | 1. 新增标签<br>2. 删除标签 |
| 输出 | 操作结果 |

#### 3.7.4 问题审核

| 需求项 | 描述 |
|--------|------|
| 功能名称 | 违规问题处理 |
| 输入 | 问题ID、操作类型 |
| 处理逻辑 | 1. 查看问题详情<br>2. 删除违规问题<br>3. 关闭违规问题 |
| 输出 | 操作结果 |

---

## 4. 非功能需求

### 4.1 性能需求

| 指标 | 要求 |
|------|------|
| 页面响应时间 | 首页加载 < 2秒 |
| API响应时间 | 普通接口 < 500ms，复杂查询 < 1秒 |
| 并发用户数 | 支持100人同时在线 |
| 数据库查询 | 单表查询 < 100ms |

### 4.2 安全需求

| 安全项 | 措施 |
|--------|------|
| 密码安全 | BCrypt加密存储，不可逆 |
| 接口认证 | JWT Token认证 |
| 接口防刷 | 同一用户1分钟内最多请求60次 |
| XSS防护 | 内容转义或使用Markdown渲染 |
| SQL注入 | 使用参数化查询（MyBatis-Plus） |
| 越权访问 | 接口校验当前用户是否有操作权限 |

### 4.3 可用性需求

| 指标 | 要求 |
|------|------|
| 系统可用性 | 99% |
| 数据备份 | 每日自动备份数据库 |
| 错误提示 | 友好的中文错误提示 |

### 4.4 可维护性需求

| 指标 | 要求 |
|------|------|
| 代码规范 | 遵循阿里巴巴Java开发规范 |
| 日志记录 | 关键操作记录日志 |
| 接口文档 | Swagger自动生成 |
| 数据库设计 | 遵循第三范式 |

---

## 5. 数据流图

### 5.1 核心业务流程

```
用户注册/登录
      ↓
发布问题 ← → 浏览问题列表 ← → 搜索问题
      ↓              ↓
  查看问题详情 ← ← ←
      ↓
发布回答 → 查看回答列表 → 采纳最佳答案
      ↓
点赞/踩 → 收藏 → 查看排行榜
```

### 5.2 采纳流程

```
问题作者查看回答列表
      ↓
找到最佳答案
      ↓
点击"采纳"
      ↓
该回答标记为is_accepted=1
      ↓
其他回答取消采纳（如有）
      ↓
问题作者获得通知（后期）
```

---

## 6. 界面原型（简述）

### 6.1 首页

- 顶部导航栏：Logo、搜索框、登录/注册按钮、用户名
- 侧边栏：分类列表
- 主体区域：问题列表（标题、摘要、浏览量、回答数、点赞数）
- 右侧：热门问题榜、优秀回答者榜

### 6.2 问题详情页

- 问题区域：标题、内容、标签、提问者信息、点赞/收藏按钮
- 回答区域：回答列表、发布回答框
- 每个回答：内容、回答者信息、点赞按钮、采纳按钮（仅问题作者可见）

### 6.3 个人中心

- 我的提问列表
- 我的回答列表
- 我的收藏列表
- 修改个人信息

---

## 7. 验收标准

| 验收项 | 标准 |
|--------|------|
| 用户模块 | 注册、登录、修改信息功能正常 |
| 问题模块 | 发布、编辑、删除、列表、详情功能正常 |
| 回答模块 | 发布、编辑、删除、采纳功能正常 |
| 互动模块 | 点赞/踩、收藏功能正常 |
| 搜索模块 | 关键词搜索功能正常 |
| 排行榜 | 热门问题、优秀回答者显示正常 |
| 管理后台 | 用户管理、分类管理、标签管理功能正常 |
| 接口安全 | 未登录访问需要登录的接口返回401 |
| 性能 | 页面加载时间 < 2秒 |
