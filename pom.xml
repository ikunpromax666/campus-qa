<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ① 聚合父工程继续继承 spring-boot-starter-parent，统一 Spring Boot 版本 -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.campus</groupId>
    <artifactId>campus-qa</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <!-- ② 关键：聚合工程必须打包为 pom -->
    <packaging>pom</packaging>
    <name>campus-qa</name>

    <modules>
        <module>campus-qa-common</module>
        <module>campus-qa-pojo</module>
        <module>campus-qa-mapper</module>
        <module>campus-qa-service</module>
        <module>campus-qa-web</module>
    </modules>

   <properties>
        <java.version>17</java.version>
        <!-- MyBatis-Plus 版本统一管理（spring-boot-starter-parent 不托管它，必须自己声明） -->
        <mybatis-plus.version>3.5.9</mybatis-plus.version>
        <!-- 强制 javac 运行注解处理器（Lombok）：
             JDK 23 起 javac 默认关闭隐式注解处理，若 Maven 跑在高版本 JDK 上
             会导致 @Data/@Getter 等静默失效，此配置显式开启（-proc:full） -->
        <maven.compiler.proc>full</maven.compiler.proc>
   </properties>

    <!-- 只做"版本声明"，不引入实际依赖：子模块声明 mybatis-plus 相关坐标时无需写 version -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-annotation</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            <!-- 3.5.9+ 分页/多租户等插件拆分到此模块，需要时显式引入 -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
