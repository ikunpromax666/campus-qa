server:
  port: 8848
spring:
  application:
    name: campus-qa-web
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_qa?useSSL=false&serverTimezone=UTC
    username: root
    password: syf1017539550

# JWT 配置
jwt:
  # HS256 密钥，长度必须 >= 32 字符（256位）；生产环境应改用环境变量/配置中心，不要提交真实密钥
  secret: campus-qa-dev-jwt-secret-2026-abc123456789
  # token 有效期（小时），72 = 3 天
  expire-hours: 72
